{
  "name": "64-pad-explorer",
  "private": true,
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest",
    "test:e2e": "npx playwright test",
    "test:prod": "BASE_URL=https://murinaikurashi.com/apps/64-pad npx playwright test"
  },
  "devDependencies": {
    "@playwright/test": "^1.50.0",
    "vitest": "^3.0.0"
  }
}
